import { anyApi } from 'convex/server'

// The generated Convex API is created by `npx convex dev` for a linked deployment.
// Keeping this reference here lets the frontend build before a deployment is linked.
export const api = anyApi as any
